server:
  port: 8070

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: admin
    url: jdbc:mysql://localhost:3307/manager?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.moore.model.entity
  global-config:
    db-config:
      id-type: auto
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
